# Alerts - Email (pick one)
RESEND_API_KEY=re_xxx
# Optional: verified sender address in Resend
RESEND_FROM=onboarding@resend.dev

# Batch agent: send alert when CRITICAL (recipient)
ALERT_EMAIL=ops@company.com
